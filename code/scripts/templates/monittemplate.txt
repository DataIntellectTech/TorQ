check process $procname
    matching \"$KDBSTACKID -proctype $proctype -procname $procname\"
    start program = \"/bin/bash $startstopsc start $procname\"
        with timeout 10 seconds
    stop program = \"/bin/bash $startstopsc stop $procname\"
    every \"* * * * *\"
    mode active

